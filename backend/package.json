{
  "name": "kulture-explora-api",
  "version": "1.0.0",
  "description": "api for kulture explora app",
  "main": "server.js",
  "scripts": {
    "prebuild": "npm i",
    "build": "npx tsc",
    "start": "node dist/server.js",
    "dev": "npm run build && concurrently \"npx tsc -w\" \"nodemon dist/server.js\""
  },
  "dependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.1",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.9",
    "typescript": "^5.7.3"
  }
}
